spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: files:////D:/Project/Learning/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true
